<Page x:Class="Travel.Hotels"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Travel"
      mc:Ignorable="d" 
      MinHeight="800" MinWidth="1200" 
      Title="Hotels" IsVisibleChanged="Page_IsVisibleChanged" Height="1086">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="83"/>
        </Grid.RowDefinitions>

        <DataGrid 
                  x:Name="ToursGrid" 
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  ColumnWidth="*"
                  HorizontalContentAlignment="Center"
                  VerticalContentAlignment="Center"  Width="928" HorizontalAlignment="Center" Margin="0,33,0,0" Height="445" VerticalAlignment="Top" SelectionChanged="ToursGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Отель" Binding="{Binding Name}" Width="250"></DataGridTextColumn>
                <DataGridTextColumn Header="Количество звезд" Binding="{Binding CountOfStars}" Width="220"></DataGridTextColumn>
                <DataGridTextColumn Header="Страна" Binding="{Binding Country.Name}" Width="240"></DataGridTextColumn>
                <DataGridTemplateColumn Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Редактировать" Name="BtnEdit" Click="BtnEdit_Click" Background="#fac716"></Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>


        <Button Content="Добавить"  Background="#fac716" HorizontalAlignment="Left" Name="BtnAdd" Click="Button_Click" Width="200" Height="50" VerticalAlignment="Bottom" Margin="137,0,0,371"/>
        <Button Content="Удалить"  Background="#fac716" x:Name="BtnDelete" Click="Button_Click_1" Width="220" Height="50" Margin="0,577,136,376" HorizontalAlignment="Right"/>

    </Grid>
</Page>
